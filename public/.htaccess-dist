# APP_ENV is assumed to be 'dev' if not set
# SetEnv APP_ENV prod

<IfModule mod_rewrite.c>
    Options -MultiViews

    RewriteEngine On
    # RewriteBase /ghis/ # if you use a non-root path

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [QSA,L]
</IfModule>
